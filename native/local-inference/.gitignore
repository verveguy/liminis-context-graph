.build/
*.o
*.d

# Generated model artifacts (large; regenerate via convert-embedding-model.py).
# Root-anchored (leading /) so this only matches convert-embedding-model.py's
# top-level output, never a directory under Tests/LocalInferenceTests/Fixtures/ —
# git cannot honor a `!` re-inclusion for files inside a directory a prior
# pattern already excluded, so an unanchored pattern here would silently
# swallow fixture content despite the negation below (see #518).
/*.mlpackage
/*.mlmodelc

# Tokenizer files (downloaded at setup time; not source code)
tokenizer.json
tokenizer_config.json
tokenizer_reference.json
vocab.txt
special_tokens_map.json
config.json

# Benchmark output (new runs write to benchmark/results/)
benchmark/results/*.json
benchmark/results/*.md

# Allow test fixtures committed under Tests/ (stub .mlpackage and tokenizer cache)
!Tests/LocalInferenceTests/Fixtures/
!Tests/LocalInferenceTests/Fixtures/**
